<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wf.shucat.mapper.CatPicMapper">

    <select id="findPicById" parameterType="integer" resultType="java.lang.String">
        select pic from cat_pic where cid=#{id}
    </select>
</mapper>